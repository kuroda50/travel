import 'package:flutter/material.dart';

class EmailChangeScreen2 extends StatelessWidget {
  const EmailChangeScreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}